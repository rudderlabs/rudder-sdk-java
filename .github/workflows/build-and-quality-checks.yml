name: Build & Code Quality Checks

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'
#      - name: Cache local Maven repository
#        uses: actions/cache@v3
#        with:
#          path: ~/.m2/repository
#          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
#          restore-keys: |
#            ${{ runner.os }}-maven-

      - name: Setup
        run: mvn clean && mvn templating:filter-sources

#      - name: Unit Test
#        run: mvn test
#
#      - name: Build
#        run: mvn install -DskipTests=true #Skipt Unit test as it has been executed previously
#
#      - name: Generate coverage report
#        uses: cicirello/jacoco-badge-generator@v2
#        with:
#          generate-branches-badge: true
#          generate-summary: true
#          jacoco-csv-file: >
#            analytics-core/target/site/jacoco/jacoco.csv
#            analytics/target/site/jacoco/jacoco.csv
#
#      - name: Upload coverage report
#        uses: actions/upload-artifact@v3
#        with:
#          name: index.html
#          path: |
#            analytics-core/target/site/jacoco/
#            analytics/target/site/jacoco/

      - name: PMD Lint checking
        run: |
            mvn pmd:pmd
            mvn pmd:cpd

      - name: Upload PMD lint report
        uses: actions/upload-artifact@v3
        with:
          name: |
            cpd.html
            pmd.html
          path: |
            analytics-core/target/site
            analytics-core/target/site
            analytics/target/site
            analytics/target/site